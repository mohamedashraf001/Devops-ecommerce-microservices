apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-redis
  namespace: ecommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-redis

  template:
    metadata:
      labels:
        app: cart-redis

    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379